// your ImageSlider code here!
import React from 'react';

// "export 'default' (imported as 'ImageSlider') was not found in './ImageSlider'

export default class ImageSlider extends React.Component {
    render() {
        return (
            <div></div>
        )
    }
}